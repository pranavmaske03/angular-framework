import { OmDirective } from './om.directive';

describe('OmDirective', () => {
  it('should create an instance', () => {
    const directive = new OmDirective();
    expect(directive).toBeTruthy();
  });
});
